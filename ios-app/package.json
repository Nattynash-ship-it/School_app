{
  "name": "study-hub-ios",
  "private": true,
  "version": "1.0.0",
  "description": "Native iPad shell around natashastudy.netlify.app. The site stays the source of truth; this only wraps it so it can be installed like an app.",
  "devDependencies": {
    "@capacitor/cli": "^8.0.0"
  },
  "dependencies": {
    "@capacitor/core": "^8.0.0",
    "@capacitor/ios": "^8.0.0"
  }
}
